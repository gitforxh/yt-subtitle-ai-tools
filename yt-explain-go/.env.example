# Server
PORT=18794
BASE_URL=http://127.0.0.1:18794

# OpenClaw bridge mode (no OAuth needed)
SESSION_KEY=ext-transcript

# Optional: if your extension sends sessionKey in request body,
# that per-request value overrides SESSION_KEY.
